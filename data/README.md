# Dataset

The full CSV is intentionally not committed because it is large.

Run:

```bash
python src/download_data.py
```

This retrieves the public OpenML copy of the classic ULB credit-card fraud benchmark and saves `creditcard.csv` here.
